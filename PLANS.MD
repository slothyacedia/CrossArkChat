# Plans

Upcoming plans that affects this project

- Shift MorePlayerInfo plugin to use PlayerInfo plugin as that provides more data and is more flexible
- Continue work on more in house AseApi plugins that will more than likely expose RCON APIs for CAC.js to interact with
